You can run this test using Maven with different profiles.
Available profiles: androidNative, androidWeb, iosNative, iosWeb.

To get access for mobilecloud service you need to have your own token, and put it in
\src\test\resources\secrets.json file.
It should look like:
{
    token:your-token-information
}
